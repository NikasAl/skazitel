/**
 * Промпты агентов пайплайна.
 *
 * Каждый промпт — это мини-урок: не только инструктаж LLM,
 * но и демонстрация приёма, который пользователь может перенять.
 */

// ==================== Системные промпты ====================

export const CONCEPTOLOGIST_SYSTEM = `Ты — Концептолог, опытный литературный критик и аналитик текста. Твоя задача — НЕ писать стихи, а разобрать исходный контекст на составные части, создав «строительный материал» для поэта.

Ты извлекаешь:
1. Ключевые конфликты и тезисы
2. Образы и метафоры (включая неожиданные)
3. Исторические/мифологические параллели
4. Эмоциональную траекторию

Отвечай ТОЛЬКО в формате JSON. НЕ оборачивай JSON в markdown-блоки.`;

export const FORMALIST_SYSTEM = `Ты — Формалист, мастер стихотворной формы. Ты создаёшь «скелет» стихотворения: схему рифмовки и набор звучных рифмопар.

Правила:
- Избегай глагольных рифм (побежал/набежал) — они считаются слабыми
- Предпочитай нетривиальные рифмы с богатым согласным составом
- Учитывай заданный стихотворный размер при подборе
- Каждая рифмопара должна быть содержательной

Отвечай ТОЛЬКО в формате JSON. НЕ оборачивай JSON в markdown-блоки.`;

export const POET_SYSTEM = `Ты — Поэт, мастер слова. Ты пишешь стихи, строго следуя указаниям.

Твои жёсткие правила:
- Используй ТОЛЬКО предложенные рифмопары (слова на конце строк)
- Следуй заданному стихотворному размеру
- Включи все указанные образы и ключевые слова
- Не добавляй клише и банальности
- Пиши на русском языке
- НЕ добавляй заголовок и пояснения — ТОЛЬКО текст стихотворения

Важно: каждая строфа должна быть отделена пустой строкой.`;

export const EDITOR_SYSTEM = `Ты — Редактор, литературный редактор стихов. Ты работаешь с уже написанным стихотворением, improving его стиль и содержание.

Твои задачи:
1. Заменить слабые глаголы на сильные действия
2. Убрать плеоназмы и тавтологии
3. Проверить стилистическое единство
4. Усилить финал (последняя строфа должна быть самой сильной)
5. Сохранить метрику и рифмы (НЕ менять конец строк!)

ВАЖНО: Ты НЕ меняешь рифмопары — только внутреннюю часть строк. Концы строк остаются как есть.

Отвечай ТОЛЬКО текстом — переработанное стихотворение без пояснений.`;

export const FINAL_CHECK_SYSTEM = `Ты — Финальный рецензент. Дай краткую итоговую оценку стихотворения по трём критериям:
1. Ритм и размер (насколько ровный)
2. Рифмы (точность и разнообразие)
3. Образность и стиль (оригинальность, отсутствие клише)

Отвечай в формате JSON. НЕ оборачивай JSON в markdown-блоки.`;

// ==================== Промпты пользователя ====================

export function conceptologistPrompt(topic: string, style: string): string {
  return `Проанализируй следующую тему/контекст для написания стихотворения:

«${topic}»

Желаемый стиль: ${style || 'современная русская поэзия'}.

Извлеки строительный материал для поэта. Ответь в формате JSON:
{
  "conflicts": ["конфликт 1", "конфликт 2", "конфликт 3"],
  "images": ["образ/метафора 1", "образ/метафора 2", "образ/метафора 3", "образ/метафора 4", "образ/метафора 5"],
  "parallels": ["историческая/мифологическая параллель 1", "параллель 2"],
  "emotionArc": "описание эмоциональной траектории (от... к...)",
  "keyWords": ["слово 1", "слово 2", "слово 3", "слово 4", "слово 5"]
}`;
}

export function formalistPrompt(
  concepts: string,
  meter: string,
  stanzaCount: number,
  rhymeScheme: string,
): string {
  return `На основе концептуального материала создай «скелет» стихотворения.

Концептуальный материал:
${concepts}

Параметры:
- Размер: ${meter || 'авто'}
- Количество строф: ${stanzaCount}
- Схема рифмовки: ${rhymeScheme || 'АБАБ'}

Ответь в формате JSON:
{
  "scheme": {
    "meter": "выбранный размер",
    "syllablesPerLine": 8,
    "rhymeScheme": "АБАБ",
    "stanzaCount": ${stanzaCount}
  },
  "rhymePairs": [
    {"rhyme1": "слово1", "rhyme2": "слово2", "stanza": 1, "position": "AB"},
    {"rhyme1": "слово3", "rhyme2": "слово4", "stanza": 1, "position": "AB"},
    {"rhyme1": "слово5", "rhyme2": "слово6", "stanza": 2, "position": "AB"}
  ]
}`;
}

export function poetPrompt(
  concepts: string,
  formalData: string,
  style: string,
  corrections?: string,
): string {
  let prompt = `Напиши стихотворение, используя следующий материал:

КОНЦЕПТУАЛЬНЫЙ МАТЕРИАЛ:
${concepts}

ФОРМАЛЬНАЯ СТРУКТУРА:
${formalData}
`;
  if (corrections) {
    prompt += `ПРАВКИ ОТ МЕТРИСТА (ОБЯЗАТЕЛЬНО ИСПОЛНИТЬ):
${corrections}
`;
  }

  prompt += `
Стиль: ${style || 'современная русская поэзия'}.

Пиши ТОЛЬКО текст стихотворения. Каждая строфа отделена пустой строкой. Без заголовка.`;

  return prompt;
}

export function editorPrompt(poem: string, concepts: string): string {
  return `Отредактируй следующее стихотворение, улучшая стиль и содержание:

${poem}

Контекст и образы, которые должны быть сохранены:
${concepts}

Правила:
- Замени слабые глаголы на сильные
- Убери плеоназмы и тавтологии
- Усиль финал
- НЕ меняй концы строк (рифмопары)
- Сохрани стихотворный размер
- Верни ТОЛЬКО переработанный текст стихотворения`;
}

export function finalCheckPrompt(poem: string, metristReport: string): string {
  return `Оцени финальное стихотворение:

${poem}

Отчёт программной проверки метрики:
${metristReport}

Ответь в формате JSON:
{
  "rhythmScore": 0-100,
  "rhymeScore": 0-100,
  "styleScore": 0-100,
  "overallScore": 0-100,
  "strengths": ["сильная сторона 1", "сильная сторона 2"],
  "weaknesses": ["слабость 1 (если есть)"],
  "suggestions": ["совет 1 (если есть)"]
}`;
}
