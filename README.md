# Сказитель

> Тренажёр стихосложения с ИИ-наставником. Учись писать стихи через геймификацию: ритм, рифма, метафора, фонетика.

## О проекте

**Сказитель** — приложение для обучения ремеслу написания стихов с помощью нейросетей. Не генератор, который пишет за тебя, а тренажёр, который развивает конкретные навыки: метрическое чутьё, рифмический словарь, конструкция метафор, управление формой.

Все данные хранятся локально. Нейросети подключаются через API-ключ пользователя (OpenRouter, GigaChat, z-ai) — приложение не имеет собственного бэкенда.

### Почему «Сказитель»

Сказитель — устный поэт, исполнитель былин и эпических сказаний. Архетип живого слова, который встаёт на путь мастерства. Это название отражает суть: не инструмент для настройки, а путь к умению формулировать мысли своей головой.

## Возможности

- **8 типов упражнений** — от ритмического тренажёра до деконструкции мастеров
- **10 встроенных тем** — работает без API-ключа, как демо
- **Система сложности 1–10** — адаптируется под уровень пользователя
- **Геймификация** — стрики, XP, уровни, достижения
- **Творческие сессии** — свободное письмо без оценки после каждых нескольких упражнений
- **Мульти-провайдер LLM** — OpenRouter, GigaChat, z-ai через единый интерфейс
- **Локальное хранение** — все данные на устройстве, импорт/экспорт в JSON
- **Редактор стихов** — с подсветкой ритма и базовыми инструментами

## Структура репозитория

```
skazitel/
├── chrome-extension/          # Chrome-плагин (новая вкладка)
│   ├── src/
│   │   ├── popup/             # UI-компоненты (React + TypeScript)
│   │   │   ├── components/    # Общие UI-компоненты
│   │   │   ├── screens/       # Экраны приложения
│   │   │   └── styles/        # Стили (CSS Modules / Tailwind)
│   │   ├── core/              # Бизнес-логика
│   │   │   ├── exercises/     # Движок упражнений
│   │   │   ├── gamification/  # Система XP, уровней, стриков
│   │   │   ├── storage/       # IndexedDB / chrome.storage
│   │   │   └── editor/        # Логика редактора стихов
│   │   ├── llm/               # LLM-интеграция
│   │   │   ├── providers/    # Адаптеры провайдеров
│   │   │   ├── prompts/       # Шаблоны промптов
│   │   │   └── models.ts      # Типы моделей и конфигурации
│   │   └── data/
│   │       ├── built-in/      # Встроенные темы и упражнения (JSON)
│   │       └── prompts/       # Промпт-шаблоны для упражнений
│   ├── public/
│   │   ├── manifest.json
│   │   └── icons/
│   ├── tests/
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── package.json
│
├── android-app/               # Android-приложение (Kotlin + Compose)
│   ├── app/
│   │   ├── src/main/
│   │   │   ├── java/ru/skazitel/
│   │   │   │   ├── ui/        # Compose-экраны и компоненты
│   │   │   │   ├── core/      # Бизнес-логика
│   │   │   │   ├── llm/       # LLM-интеграция
│   │   │   │   ├── storage/   # Room + DataStore
│   │   │   │   └── editor/    # Редактор стихов
│   │   │   ├── res/
│   │   │   └── AndroidManifest.xml
│   │   └── build.gradle.kts
│   └── build.gradle.kts
│
├── docs/                      # Документация проекта
│   ├── ARCHITECTURE.md
│   ├── EXERCISES.md
│   └── ROADMAP.md
│
├── shared-concepts/           # Общие концепции и спецификации
│   ├── exercise-types.yaml    # Определения типов упражнений
│   ├── difficulty-levels.yaml # Уровни сложности
│   ├── builtin-themes/        # Встроенные темы (JSON)
│   │   ├── seasons.json
│   │   ├── city-life.json
│   │   ├── relationships.json
│   │   ├── memory-childhood.json
│   │   ├── work-profession.json
│   │   ├── travel.json
│   │   ├── technology-ai.json
│   │   ├── nature.json
│   │   ├── loss.json
│   │   └── hope-future.json
│   └── prompt-templates/     # Промпт-шаблоны (Markdown)
│       ├── system-prompt.md
│       ├── exercise-gen.md
│       ├── review.md
│       └── creative-session.md
│
├── .github/
│   └── ISSUE_TEMPLATE/
├── .gitignore
└── README.md
```

## Быстрый старт (Chrome-плагин)

### Требования

- Node.js >= 18
- npm >= 9
- Chrome / Edge / Chromium-based браузер

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/NikasAl/skazitel.git
cd skazitel/chrome-extension

# Установка зависимостей
npm install

# Сборка для разработки
npm run dev

# Сборка для продакшена
npm run build
```

### Загрузка в Chrome

1. Откройте `chrome://extensions/`
2. Включите «Режим разработчика»
3. Нажмите «Загрузить распакованное расширение»
4. Выберите папку `chrome-extension/dist`
5. При открытии новой вкладки будет отображаться Сказитель

### Настройка API

1. Откройте Сказитель в новой вкладке
2. Перейдите в Настройки
3. Выберите провайдера и введите API-ключ
4. Выберите модель из выпадающего списка
5. Начните тренировку

## Технологический стек

### Chrome-плагин

| Слой | Технология |
|------|-----------|
| UI | React 18 + TypeScript |
| Сборка | Vite |
| Стилизация | Tailwind CSS + CSS Modules |
| Хранение | IndexedDB (через Dexie.js) |
| LLM | Fetch API (REST) |
| Тестирование | Vitest + React Testing Library |

### Android-приложение (план)

| Слой | Технология |
|------|-----------|
| UI | Jetpack Compose |
| Язык | Kotlin |
| Сборка | Gradle (Kotlin DSL) |
| Хранение | Room + DataStore |
| Сеть | Retrofit + OkHttp |
| Навигация | Compose Navigation |
| DI | Koin |
| Тестирование | JUnit + Compose Testing |

## Концепция работы

```
Пользователь выбирает тему
         |
    ┌────┴────┐
    │ API-ключ │         Нет API-ключа
    │ доступен? ├──────────────────────┐
    └────┬────┘                       |
         Да                           |
    LLM генерирует          Встроенные темы
    упражнение              из репозитория
         |                       |
         └───────────┬───────────┘
                     |
            Пользователь
            выполняет задание
                     |
            LLM проверяет ответ
            (или встроенная проверка)
                     |
          Фидбек + XP + прогресс
                     |
          Каждое 5-е упражнение:
          Творческая сессия
```

## Документация

- [Архитектура](docs/ARCHITECTURE.md) — техническая архитектура, модели данных, API-слой
- [Система упражнений](docs/EXERCISES.md) — типы упражнений, промпты, сложность
- [Roadmap](docs/ROADMAP.md) — план разработки

## Лицензия

MIT
